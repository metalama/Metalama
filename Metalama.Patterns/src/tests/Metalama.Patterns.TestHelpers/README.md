This package contains helper classes used, together with the `Metalama.Patterns.Caching.TestHelpers`, when developing unit tests for new `CachingBackend` implementations.

Unless you are developing your own `CachingBackend`, you don't need this package.